# onelap-strava-sync skill

This folder contains distribution-ready skill artifacts for global agent config.

- Runtime code remains in repository root source directories.
- Do not copy or fork business logic into `skills/`.
- Update command examples in `resources/commands.md` when CLI changes.

See `docs/skills-mapping.md` for skill-to-code mapping.
